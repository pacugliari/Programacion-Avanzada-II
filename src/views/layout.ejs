<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title || 'Mi Sitio de Películas' %></title>

    <!-- Bootstrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Material Icons -->
    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"
    />

    <!-- Sweetalert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <!-- CSS -->
    <link rel="stylesheet" href="/css/layout.css" />
    <link rel="stylesheet" href="/css/index.css" />

    <!-- JS -->
    <script src="/js/layout.js"></script>
  </head>
  <body class="bg-secondary min-vh-100">
    <header class="p-3 bg-dark text-white">
      <div class="container">
        <div class="row align-items-center">
          <div class="col-12 col-md-6 text-center text-md-start mb-2 mb-md-0">
            <h1 class="mb-0">
              <a href="/movies" class="text-white text-decoration-none"
                >🎬 Trailerflix</a
              >
            </h1>
          </div>

          <% if (user) { %>
          <div
            class="col-12 col-md-6 d-flex justify-content-center justify-content-md-end align-items-center gap-3"
          >
            <span class="text-white">👤 <%= user.name %></span>
            <form action="/auth/logout" method="POST" class="d-inline">
              <button type="submit" class="btn btn-outline-light btn-sm">
                Cerrar sesión
              </button>
            </form>
          </div>
          <% } %>
        </div>
      </div>
    </header>

    <main
      class="container mt-4 d-flex align-items-center justify-content-center"
    >
      <%- body %>
    </main>

<footer class="mt-2 text-center text-muted">
  <p>
    &copy; <%= new Date().getFullYear() %> Trailerflix. Todos los derechos reservados.
  </p>
  <p>
    Desarrollado por 
    <a href="https://www.linkedin.com/in/pablo-alberto-cugliari-b092b9269/" target="_blank" rel="noopener noreferrer">
      Pablo Alberto Cugliari
    </a>
  </p>
</footer>
  </body>
</html>
